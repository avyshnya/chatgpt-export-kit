# START-PROMPT — скопіюй усе, що нижче риски, у Claude Code

Відкрий термінал у теці цього кіта, запусти `claude`, встав цей промпт
цілком (від риски до кінця файлу) і надішли.

---

Ти ведеш інтерактивне вивантаження моєї історії ChatGPT у локальний архів.

Спочатку прочитай ПОВНІСТЮ два файли в цій теці:
1. `PLAYBOOK.md` — покроковий сценарій, якого ти дотримуєшся ДОСЛІВНО.
2. `KNOWLEDGE.md` — довідник ендпоінтів і граблів (лімітер, пастки, помилки).

Потім веди мене за сценарієм PLAYBOOK. Ключові правила (вони ж у PLAYBOOK,
це нагадування):

- НАЙПЕРШЕ запитай мову діалогу: 1. Українська / 2. English — і далі весь
  інтерактив веди обраною мовою.
- Всі питання й вибори став простими нумерованими пунктами (1/2/3). Якщо в
  сесії доступний інтерактивний інструмент вибору (AskUserQuestion) —
  використовуй його; якщо ні — нумерований список текстом.
- ПЕРЕД кожним етапом, що тягне дані: оголоси, що збираєшся зробити; дешевою
  розвідкою порахуй фактичний обсяг; покажи оцінку часу діапазоном «від X до
  Y»; запитай — виконати / пропустити етап / зупинитися. Пропуск етапу нічого
  не ламає — пропущене можна долити пізніше.
- Темп диктує лімітер OpenAI: не форсуй, на 429 — тільки подовжуй інтервали.
  Чесно попереджай мене про час.
- Дані (ідентифікатори) ніколи не переписуй руками в чат — лише технікою
  передачі з PLAYBOOK (sha256-звірка).
- Готові скрипти-шаблони лежать у `tools/` — використовуй їх, не переписуй
  з нуля.
- Наприкінці: фінальний звіт (де все лежить + структура + таблиця покриття),
  нагадування про приватність share-лінків, і покажи мені промпт для аналізу
  архіву з файлу `ANALYZE-PROMPT.md`.

Почни з питання про мову.
